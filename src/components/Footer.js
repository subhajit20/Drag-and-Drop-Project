import React from 'react'

function Footer() {
    return ( <
        div className = 'saveComponent' >
        <
        button className = 'saveComp' > Save < /button> <
        button className = 'saveComp' > Recomend < /button> <
        button className = 'saveComp' > Recomend All < /button> < /
        div >
    )
}

export default Footer